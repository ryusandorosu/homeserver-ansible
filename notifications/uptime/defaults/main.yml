ping_retry_command: "for i in {1..30}; do ping -c1 8.8.8.8 && break; sleep 5; done;"
nc_retry_command: "for i in {1..30}; do nc -zv {{ localhost }} {{ docker.mihomo.port.socks5 }} && exit 0; sleep 2; done; exit 1"
