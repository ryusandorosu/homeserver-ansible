all:
  hosts:
    localhost:
      ansible_connection: local

  children:
    local:
      hosts:
        localhost:
    remote:
      hosts:
        nuc-server:
          ansible_host: "{{ wireguard_server_ip }}"

    server:
      children:
        local:
        remote:

    windows:
      hosts:
        asus:
          ansible_host: "{{ wireguard_devices.large_laptop.ip }}"
        honor:
          ansible_host: "{{ wireguard_devices.laptop.ip }}"