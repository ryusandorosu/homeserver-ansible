### wsl
- name: Create custom VSCode tasks directory in HOME workspace
  file:
    path: "{{ homedir }}/.vscode"
    state: directory
    owner: "{{ user }}"
    group: "{{ user }}"
    mode: '0755'

- name: Deploy VSCode task enabling 'tools/j2render.sh'
  template:
    src: tasks.json.j2
    dest: "{{ homedir }}/.vscode/tasks.json"
    owner: "{{ user }}"
    group: "{{ user }}"
    mode: '0644'

### windows (delegated)
- name: Resolve current Windows host alias
  set_fact:
    windows_target_host: >-
      {{
        groups['windows']
        | select('match', '^local-')
        | map('extract', hostvars)
        | selectattr('wsl_hostname', 'defined')
        | selectattr('wsl_hostname', 'equalto', ansible_hostname)
        | map(attribute='inventory_hostname')
        | first
      }}

- set_fact:
    whost_AppData: "{{ hostvars[windows_target_host].ansible_facts.env.APPDATA }}"

## imported from windows roles
- name: Read remote keybindings before deploy
  ansible.windows.slurp:
    path: "{{ whost_AppData }}\\Code\\User\\keybindings.json"
  delegate_to: "{{ windows_target_host }}"
  register: before_keybind
  ignore_errors: true

- name: Render repo keybindings
  set_fact:
    rendered_keybind: "{{ lookup('file', 'files/keybindings.json') }}"

- name: Show config unified diff 
  delegate_to: localhost
  vars:
    before: "{{ before_keybind.content | b64decode }}"
    after: "{{ rendered_keybind }}"
  shell: |
    {{ lookup('template', inventory_dir ~ '/../windows/templates/pyshell.sh.j2') }}
  ignore_errors: true
## /imported from windows roles

- name: Deploy VSCode task keybinding to run 'tools/j2render.sh'
  win_copy:
    src: keybindings.json
    dest: "{{ whost_AppData }}\\Code\\User\\keybindings.json"
  delegate_to: "{{ windows_target_host }}"

# ## insert
# - name: Read existing keybindings.json
#   ansible.windows.slurp:
#     path: "{{ whost_AppData }}\\Code\\User\\keybindings.json"
#   delegate_to: "{{ windows_target_host }}"
#   register: _existing_keybindings_raw
#   failed_when: false

# - debug:
#     msg: "{{ _existing_keybindings_raw.content | b64decode }}"
# - debug:
#     msg: "{{ _existing_keybindings_raw.content | b64decode | from_json }}"

# - name: Parse existing keybindings (default to empty list if file absent)
#   set_fact:
#     existing_keybindings: "{{ (_existing_keybindings_raw.content | b64decode | from_json) if _existing_keybindings_raw.content is defined else [] }}"

# - name: Define desired keybinding
#   set_fact:
#     desired_keybinding:
#       key: "ctrl+shift+a"
#       command: "workbench.action.tasks.runTask"
#       when: "editorFocus"
#       args: "Render Ansible Template"

# - name: Merge keybinding if not already present
#   set_fact:
#     merged_keybindings: >-
#       {{ existing_keybindings
#          + ([desired_keybinding] if (existing_keybindings | selectattr('command', 'equalto', desired_keybinding.command) | list | length == 0) else []) }}

# - name: Deploy merged keybindings.json
#   copy:
#     content: "{{ merged_keybindings | to_nice_json }}"
#     dest: "{{ whost_AppData }}\\Code\\User\\keybindings.json"
#   delegate_to: "{{ windows_target_host }}"
# ## /insert
