- name: Add extra binary directories to sudo secure_path
  copy:
    content: 'Defaults secure_path="{{ (sudo_secure_paths + default_pathenv) | join(":") }}"'
    dest: /etc/sudoers.d/extra-secure-path
    owner: root
    group: root
    mode: '0440'
    validate: '/usr/sbin/visudo -cf %s'
