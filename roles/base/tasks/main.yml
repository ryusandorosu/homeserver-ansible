- name: Install base packages
  apt:
    name:
      - git
      - curl
      - jq
      - tree
      - far2l
      - tcpdump
      - rsync
      - ca-certificates
      - unattended-upgrades
    state: present
    update_cache: yes
