nginx_labels:
  vhost:
    operator: '=~'
  method:
    operator: '=~'
  ip:
    operator: '!~'
    default: "{{ home_ip }}"

nginx_filters: >-
  {{
    nginx_labels
      | dict2items
      | map(attribute='key')
      | zip(
          nginx_labels
          | dict2items
          | map(attribute='value.operator')
        )
      | map('join', ' ')
      | zip(
          nginx_labels
          | dict2items
          | map(attribute='key')
        )
      | map('join', ' "$')
      | list
  }}
