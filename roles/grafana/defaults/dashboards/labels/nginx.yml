nginx_labels:
  - vhost
  - method
  - status
  - ip
  - agent
  - exclude_ip

nginx_exclude_ip:
  - "{{ home_ip }}"

nginx_filters: >-
  {% for filter in nginx_labels | reject('contains', 'exclude') %}{{ filter }} =~ "${{ filter }}", {% endfor %}
  exclude_ip !~ "$exclude_ip"
