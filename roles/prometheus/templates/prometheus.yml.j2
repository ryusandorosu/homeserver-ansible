global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "node"
    static_configs:
      - targets:
          - "{{ docker.node_exporter }}:{{ node_exporter_port }}"
        labels:
          instance: "{{ inventory_hostname }}"
