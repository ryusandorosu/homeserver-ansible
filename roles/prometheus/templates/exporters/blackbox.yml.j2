modules:
  http_2xx:
    prober: http
    timeout: 5s
    http:
      preferred_ip_protocol: "ip4"
      proxy_url: socks5h://{{ docker.mihomo }}:{{ socks5_port }}
      valid_status_codes: []
      method: GET
