[Service]
ExecStartPost=
ExecStartPost=/bin/bash -c '{{ telegram_script }} "🟢 {{ item }} service started" status "$0" || true'

ExecStopPost=
ExecStopPost=/bin/bash -c '{{ telegram_script }} "🔴 {{ item }} service stopped" status "$0"'
