services:
  authelia:
    image: authelia/authelia:latest
    container_name: {{ docker.nginx_auth }}
    restart: unless-stopped
    volumes:
      - {{ authelia_config_dir }}:/config
      - {{ authelia_data_dir }}:/data
    network_mode: host
