return {
  {
    "nvim-treesitter/nvim-treesitter",
    build=":TSUpdate",
    config = function()
      require("nvim-treesitter").setup({
        ensure_installed = { {% for item in treesitter %}"{{ item }}"{% if not loop.last %}, {% endif %}{% endfor %} },
      })
    end
  },
}
