- name: Install nginx
  apt:
    name:
      - nginx
      - libnginx-mod-stream
    state: present
    update_cache: yes

- name: Enable nginx
  systemd:
    name: nginx
    enabled: yes
    state: started

- name: Deploy nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf
  notify: validate and reload nginx

- name: Deploy nginx sites
  template:
    src: site.j2
    dest: "/etc/nginx/sites-available/{{ item.server_name }}"
  loop: "{{ nginx_sites }}"
  notify: validate and reload nginx

- name: Enable sites
  file:
    src: "/etc/nginx/sites-available/{{ item.server_name }}"
    dest: "/etc/nginx/sites-enabled/{{ item.server_name }}"
    state: link
  loop: "{{ nginx_sites }}"

- name: Deploy status page config
  template:
    src: status.conf.j2
    dest: /etc/nginx/sites-available/status.conf
  notify: validate and reload nginx

- name: Enable status page
  file:
    src: /etc/nginx/sites-available/status.conf
    dest: /etc/nginx/sites-enabled/status.conf
    state: link

- name: Replace cockpit motd
  copy:
    content: "Web console: https://{{ 'cockpit.' ~ domain }}"
    dest: "/run/cockpit/active.issue"
