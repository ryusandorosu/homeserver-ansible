- name: Install logrotate
  apt:
    name: logrotate
    state: present
    update_cache: yes

- name: Deploy logrotate configs
  copy:
    src: "{{ item }}"
    dest: "/etc/logrotate.d/{{ item }}"
    owner: root
    group: root
    mode: '0644'
  loop:
    - nginx
