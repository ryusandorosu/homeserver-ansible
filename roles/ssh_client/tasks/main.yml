- name: Ensure .ssh dir
  file:
    path: /home/{{ user }}/.ssh
    state: directory
    mode: '0700'
    owner: "{{ user }}"

- name: Validate SSH config
  assert:
    that:
      - host_item.hostname == host_item.name
    fail_msg: "GitHub host is different from its hostname"
  loop: "{{ ssh_hosts }}"
  loop_control:
    loop_var: host_item
  when: host_item.hostname == "github.com"

- name: Deploy SSH config
  template:
    src: config.j2
    dest: /home/{{ user }}/.ssh/config
    mode: '0600'
    owner: "{{ user }}"
