- name: Install vim
  apt:
    name:
    - vim
    - vim-gtk3
    state: present

- name: Create plugins directory
  file:
    path: "/home/{{ user }}/{{ vimplugins }}"
    state: directory
    owner: "{{ user }}"
    group: "{{ user }}"
    mode: '0755'

- name: Clone vim plugins
  become_user: "{{ user }}"
  git:
    repo: "{{ item }}"
    dest: "/home/{{ user }}/{{ vimplugins }}/{{ item | basename | regex_replace('.git','') }}"
  loop:
    - https://github.com/tpope/vim-fugitive.git
    - https://github.com/airblade/vim-gitgutter.git
    - https://github.com/vim-airline/vim-airline.git

- name: Deploy vimrc
  copy:
    src: .vimrc
    dest: "/home/{{ user }}"
    owner: "{{ user }}"
    group: "{{ user }}"
    mode: '0644'

- name: Create root symlink for vimrc
  file:
    src: "/home/{{ user }}/.vimrc"
    dest: /root/.vimrc
    state: link

- name: Create root symlink for vim plugins
  file:
    src: "/home/{{ user }}/.vim"
    dest: "/root/.vim"
    state: link
