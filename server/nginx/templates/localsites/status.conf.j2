server {
    listen {{ const_port.http }};
    server_name _;

    location /stub_status {
        stub_status;
        allow {{ localhost }};
        allow {{ docker_monitoring_network }};
        deny all;
    }
}
