server {
    listen {{ http_port }};
    server_name _;

    location /stub_status {
        stub_status;
        allow {{ localhost }};
        allow {{ docker_monitoring_network }};
        deny all;
    }
}
