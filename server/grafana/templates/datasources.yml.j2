apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://{{ docker.loki.name }}:{{ docker.loki.port }}
    isDefault: true
    jsonData:
      derivedFields:
        - matcherRegex: '([0-9]{1,3}(?:\.[0-9]{1,3}){3})'
          name: IP Info
          url: 'https://ipinfo.io/$${__value.raw}'
    timeout: 300
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://{{ docker.prometheus.name }}:{{ internal_port.prometheus }}
