docker_upd_fields:
  timestamp:
    bash: "{{ customlog_dateformat }}"
  service:
    bash: service
    label: true
  status:
    bash: status
    label: true
  old_version:
    bash: old_version
  new_version:
    bash: new_version
  old_id:
    bash: old
  new_id:
    bash: new
