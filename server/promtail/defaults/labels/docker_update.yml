docker_upd_fields:
  timestamp:
    bash: "{{ customlog_dateformat }}"
  service:
    bash: service
    label: true
  status:
    bash: status
    label: true
  old_version:
    bash: old_ver
  new_version:
    bash: new_ver
  old_id:
    bash: old_id
  new_id:
    bash: new_id
